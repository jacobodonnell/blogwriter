# Up Next

## Phase 1: Foundation Setup

Fresh Laravel 12 install. No starter kit. Need to build from scratch:

1. **Install frontend dependencies** — Alpine.js, Alpine AJAX, DaisyUI
2. **Set up authentication** — Fortify + custom Alpine AJAX forms (no Livewire)
3. **Create admin layout** — Navigation, sidebar, responsive shell
4. **Create `config/blogwriter.php`** — Site and author settings
5. **Build Article model + migration** — First content type

### Current State
- Fresh Laravel 12 with default User model
- SQLite database with default tables only
- Tailwind CSS v4 installed via Vite
- No Alpine.js, no DaisyUI, no auth, no admin routes